# Agregar dentro del VirtualHost de rrhh.copelco.com.ar.conf
# (antes del </VirtualHost>)

ProxyRequests Off
ProxyPreserveHost On

<Location /procedimientos>
    ProxyPass http://localhost:3001/procedimientos
    ProxyPassReverse http://localhost:3001/procedimientos
</Location>
